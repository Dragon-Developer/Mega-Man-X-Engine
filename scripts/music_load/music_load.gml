function music_load() {
	// File Name
	/*
	var fname = "Music/" + argument[0];
	// Stream Audio
	var snd = audio_create_stream(fname + ".ogg");
	// Save into the Map
	global.music_streamed_map[? argument[0]] = snd;
	// Get Loop Start Time
	var start = -1; // No loop
	ini_open(fname + ".ini");
	start = ini_read_real("INFO", "Start", start);
	ini_close();
	music_config(argument[0], start);
	ds_list_add(global.music_playing, [global.music_hidden, snd]);
	log(audio_sound_get_track_position(global.music_hidden));*/


}
